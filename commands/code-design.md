---
name: code-design
description: Apply function-level code design principles — pure functions, single responsibility, guard clauses, side-effect management
---

Run the code-design skill to audit function-level code quality: check for deeply nested code, mixed responsibilities, impure functions mixing logic with side effects, missing guard clauses, and high cognitive load. Apply the 5-step checklist (Purity → Responsibility → Control Flow → Side Effects → Names/Variables) to each function being written or reviewed. Pair with folder-architecture for full coverage (file-level + function-level).
