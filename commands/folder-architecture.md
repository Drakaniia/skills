---
name: folder-architecture
description: Enforce clean folder organization and file placement
---

Activate the folder-architecture skill to enforce clean folder organization before creating or modifying files. Check target directory health (file count, nesting depth, dumping grounds), validate file placement (root check, config check, test mirroring), enforce naming conventions, and verify import hygiene (ordering, barrel file updates, circular dependency check).
